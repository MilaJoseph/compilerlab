%{
#include<stdio.h>
int vowels=0,cons=0;
%}
%option noyywrap
%%
[aeiouAEIOU] {vowels++;}
[a-zA-Z] {cons++;}
\n {return 0;}
%%
int main()
{
        printf("Enter the string: ");
        yylex();
        printf("\nVowels=%d\nCons=%d",vowels,cons);
        return 0;
}
